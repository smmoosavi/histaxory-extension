import React, { PropsWithChildren } from 'react';
import { createStyles } from 'src/shared/styles';

interface OwnProps {}

export type Props = PropsWithChildren<OwnProps>;

export function {{pascalCase name}}(props: Props) {
  return null;
}

const sx = createStyles({
  root: {},
});
