import { styled } from '@mui/material';
import { ComponentProps } from 'react';

export interface OwnProps {}

export type Props = ComponentProps<typeof {{pascalCase name}}>;

export const {{pascalCase name}} = styled('div', { name: '{{pascalCase name}}' })({
  /* styles */
});
